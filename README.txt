This program uses the windows class INPUT contained in the windows.h header file to mimic user keyboard inputs. It reads from a text file for a list of commands which it then executes in order before closing.

WARNING: improper use of this may cause issues with your machine
